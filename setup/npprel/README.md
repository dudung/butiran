# npprel Protocol Setup
Run `npprel-install.bat` once after cloning this repo to enable `npprel://` links.


## Steps
1. Double-click `npprel-install.bat`.
2. Make sure Python is installed.
3. Done — your `npprel://content/post/...` links now open in Notepad++.
