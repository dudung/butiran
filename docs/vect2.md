# vect2
A 2-d vector


## instantiation
Syntax

`Vect2([x[, y]])`

+ `x`: x-coordinate, e.g. `1.5`, default `0`.
+ `y`: y-coordinate, e.g. `-2`, default `0`.

Code

```python
r = Vect2(y=-2)
print(r)
```

Output

```json
{ "x": 0, "y": -2 }
```